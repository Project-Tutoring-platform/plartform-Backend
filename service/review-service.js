const {  Review } = require('../models')

const reviewService = {
  // postReview: req => {
  //   const { text, point, } = req.body
  //   const id = req.query
  //   Review.findByPk(id)
  //   .then(review => {
  //     if(review) 
  //   })
  // }
}
module.exports = reviewService
